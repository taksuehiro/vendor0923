FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir -r requirements.txt

# 修正版 CMD（ログ出力を明示し、正しいモジュールを指定）
CMD ["uvicorn", "backend.main_app:app", "--host", "0.0.0.0", "--port", "8080", "--log-level", "info", "--access-log"]
